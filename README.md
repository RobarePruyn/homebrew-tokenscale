# homebrew-tokenscale

Homebrew tap for [tokenscale](https://github.com/RobarePruyn/tokenscale). Formulae here are auto-published by `dist` on every release.

## Usage

```bash
brew tap RobarePruyn/tokenscale
brew install tokenscale
```
